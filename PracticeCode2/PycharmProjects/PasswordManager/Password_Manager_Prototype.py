class passwordBox ():        password = {}        def __init__ (self, namePassedIn):                self.name = namePassedIn                        def keyholder(self, password):                print("Enter the password you would like to store")                key = input("Enter Account Name: ")                value = input("Enter Password: ")                self.passwor[key] = value                startUp(password)		        def searchPrompt(self):                prompt = input("Would you like to search for stored passwords? Y/N ")                if prompt.upper() == 'Y':                        return searchKey(password)                elif prompt.upper() == 'N':                        print("Thanks, program will now close")                        quit()                else:                        print("This is not a valid option! Please try again")                        return searchPrompt()        def searchKey():                sKey = input("Enter name of account you would like to view: ")                if sKey in password:                        print(password[sKey])                        return startUp(password)                elif sKey not in password:                        print("There is no such password stored")                        return startUp(password)			        def startUp():                start = input("Would you like to save a new password? Y/N ")                if start.upper == 'Y':                        print("Okay!")                        return keyholder(password)                elif start.upper():                        print("Okay!")                        return searchPrompt(self)                else:                        print("This is not a valid option! Please try again. ")                        return startUp(password)						newShit = passwordBox("raul")passwordBox.startUp()new = passwordBox()